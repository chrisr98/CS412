Git lab 1

Part I

(1)
$ delay 2000
$ show goal
$ git commit
$ git commit

(2)
$ delay 2000
$ show goal
$ objective
$ git checkout -b bugFix

(3)
$ delay 2000
$ show goal
$ git checkout -b bugFix
$ git commit
$ git checkout master
$ git commit
$ objective
$ git merge
I expect at least 1 argument(s) for with git merge
$ git merge bugFix

(4)
$ delay 2000
$ show goal
$ git checkout -b bugFix
$ git commit
$ git checkout master
$ git commit
$ git checkout bugFix
$ git rebase master


Part II

(1)
$ delay 2000
$ show goal
$ objective
$ git checkout C4

(2)
$ delay 2000
$ show goal
$ git checkout bigFix^
The ref bigFix^ does not exist or is unknown
$ git checkout bugFix^

(3)
$ delay 2000
$ show goal
$ git brach -f master C6
The command "git brach -f master C6" isn't supported, sorry!
$ git branch -f master C6
$ git branch -f bugFix C0
$ git checkout C1

(4)
$ delay 2000
$ show goal
$ git reset local~1
$ git checkout pushed
$ git revert pushed

Part III

(1)
$ delay 2000
$ show goal
$ objective
$ git cherry-picked C3 C4 C7
The command "git cherry-picked C3 C4 C7" isn't supported, sorry!
$ git cherry-pick C3 C4 C7

(2)
$ delay 2000
$ show goal
$ git rebase -i HEAD~4
